## Minimize manufacturing cost
a sequence of items with cost: $a_1, a_2, a_3, ...$
need to:
1. select $(l, r)$ a time,
2. cost(l, r) = $C + \sum_{i=l}^r a_i$
3. manufacture all items with least cost.
